# Local dev environment / private planning notes (may reference local paths)
CLAUDE.md

# Design mockups and raw screenshots kept outside docs/ during development
# (curated, cropped copies used by README.md live in docs/screenshots/)
/*.png
/*.html

# Dev-only data dump tooling, not part of the shipped addon
/tools/
